# HTTP port for the API
PORT=4000

# PostgreSQL connection string used by the API and migrations
DATABASE_URL=postgres://postgres:postgres@localhost:5432/articles_fs

# Optional: turn on verbose SQL logs
DB_LOGGING=false

# Attachment storage directories
DATA_DIR=./data
ATTACHMENTS_DIR=./data/uploads

# Enable SSL for Postgres connections if your provider requires it
# PGSSLMODE=require
# DB_SSL=true
